tools/.isml-cache/
node_modules/
# `bml --examples` writes here. It is the tool OUTPUT, and committing it
# made `bml --examples` refuse in a fresh clone: the directory it wanted to
# create was already there.
bml-examples/
